version: "3.8"
services:
  db:
    image: postgres
    environment:
      POSTGRES_PASSWORD: pass123
      POSTGRES_USER: user123
      POSTGRES_DB: db123
    ports:
      - 5432:5432

  app:
    image: my-express-backend
    ports:
      - 3000:3000
    depends_on:
      - db

